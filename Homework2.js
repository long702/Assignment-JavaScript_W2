let number = 7;
let result = (number %2 === 0) ? "Even" : "1.Odd";
console.log(result);
// 1. Define a number and check if it's even or odd
let age = 20;
let status = (age >= 18) ? "2.Adult" : "Minor";
console.log(status);       
// 2. Define a person's age and check if they are an adult or minor
let isStudent = true;
let lable = isStudent ? "3.Student" : "Non-Student";
console.log(lable);
// 3. Define a boolean variable and assign a label based on its value
let score = 45;
let grade = (score >= 50) ? "Pass" : "4.Fail";
console.log(grade);
// 4. Define a score and assign a grade based on the score
let temperature = 38;
let weather = (temperature > 35) ? "5.Too Hot" : "normal";
console.log(weather);  

